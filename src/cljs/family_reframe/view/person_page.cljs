(ns family-reframe.view.person-page
  (:require [re-frame.core :as re-frame]
            [family-reframe.subs]
            [family-reframe.view.html :as html]))

(defn person-page []
  [:div "Person page"])